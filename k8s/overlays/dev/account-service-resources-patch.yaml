apiVersion: apps/v1
kind: Deployment
metadata:
  name: account-service  # nome do Deployment exato
spec:
  template:
    spec:
      containers:
      - name: account-service
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "200m"
            memory: "128Mi"
