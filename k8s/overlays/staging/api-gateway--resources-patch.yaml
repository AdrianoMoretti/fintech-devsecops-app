apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway  # nome do Deployment exato
spec:
  template:
    spec:
      containers:
      - name: api-gateway
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "200m"
            memory: "128Mi"
