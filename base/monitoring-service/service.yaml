apiVersion: v1
kind: Service
metadata:
  name: monitoring-service-service
spec:
  type: ClusterIP
  selector:
    app: monitoring-service
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
