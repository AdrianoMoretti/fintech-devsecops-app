apiVersion: v1
kind: Service
metadata:
  name: fraud-detection-service-service
spec:
  type: NodePort
  selector:
    app: fraud-detection-service
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30005
