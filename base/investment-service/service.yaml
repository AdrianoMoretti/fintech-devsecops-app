apiVersion: v1
kind: Service
metadata:
  name: investment-service-service
spec:
  type: ClusterIP
  selector:
    app: investment-service
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
