apiVersion: v1
kind: Service
metadata:
  name: transaction-service-service
spec:
  type: ClusterIP
  selector:
    app: transaction-service
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
