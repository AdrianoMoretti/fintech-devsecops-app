apiVersion: apps/v1
kind: Deployment
metadata:
  name: transaction-service  # nome do Deployment exato
spec:
  template:
    spec:
      containers:
      - name: transaction-service
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "200m"
            memory: "128Mi"
