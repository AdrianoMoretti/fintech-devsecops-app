apiVersion: v1
kind: Service
metadata:
  name: payment-service-service
spec:
  type: NodePort
  selector:
    app: payment-service
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
