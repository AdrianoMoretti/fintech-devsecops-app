apiVersion: v1
kind: Service
metadata:
  name: wallet-service-service
spec:
  type: NodePort
  selector:
    app: wallet-service
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
    nodePort: 30013
